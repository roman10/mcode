1. resumed session remains starting state. state indicator can be incorrect.
[upstream] 3. Claude Code plan mode input wrapping doesn't reflow on SIGWINCH. mcode PTY resize chain verified correct (PTY and xterm cols always match).
6. kanban mode diff viewer — opening a diff from Changes tab in kanban mode falls back to regular file viewer instead of showing a proper diff view

--- launch readiness (fix before promoting further) ---
9. set GitHub repo metadata — description, topic tags (claude-code, electron, macos, developer-tools, ai-coding, terminal, tiling, multi-agent), social preview image

